import { PrecedeByPipe } from './precede-by.pipe';

describe('PrecedeByPipe', () => {
  it('create an instance', () => {
    const pipe = new PrecedeByPipe();
    expect(pipe).toBeTruthy();
  });
});
